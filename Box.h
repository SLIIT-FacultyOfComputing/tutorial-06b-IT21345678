class Box {
    private:
       int length;
       int width;
       int height;
    public:
       void setvalues(int len,int wid,int hei);
       int getHeight();
      int getLength();
      int getWidth();
       int calcVolume();
};
